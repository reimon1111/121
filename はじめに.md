# 青木玲門 公式かもしれないサイト｜制作データ

このZIPには、現在公開している自己紹介サイトの編集用データ一式が入っています。

## 主な編集ファイル

- `app/page.tsx`
  - HTMLに相当するページ本体です。
  - 文章、見出し、プロフィール、ボタンの動作などを編集できます。
- `app/globals.css`
  - サイト全体のCSSです。
  - 色、文字サイズ、余白、PC・スマホ表示などを編集できます。
- `public/images/`
  - サイト内で使用している画像一式です。
- `app/layout.tsx`
  - ページタイトルなど、サイト全体の基本設定です。

## 技術構成

このサイトは、1枚の静的HTMLではなく、React／TypeScriptを使った構成です。

- React
- TypeScript
- Vinext／Vite
- CSS

アンケートの判定や作品画像の切り替えなど、動きのある部分も含まれています。

## パソコンで確認する方法

Node.jsが入った環境で、フォルダ内から次の順に実行します。

```bash
npm ci
npm run dev
```

表示されたURLをブラウザで開くと確認できます。

## 通常のレンタルサーバーで使う場合

現在のデータはReact用のため、そのままFTPでアップロードする形式ではありません。
一般的なレンタルサーバー向けの `index.html`・`style.css`・`script.js` 形式が必要な場合は、静的HTML版への変換が必要です。
